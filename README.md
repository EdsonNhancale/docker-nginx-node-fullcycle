# docker-nginx-node-fullcycle

``` Nodejs server with Nginx as reverse proxy ```

## How to pull from Docker Hub

``` docker pull edsonnhancale/nginx-proxy ```

``` docker pull edsonnhancale/node-nginx```

## How to run

``` docker-compose up -d ```

## How to test
``` curl http://localhost:8080 ```